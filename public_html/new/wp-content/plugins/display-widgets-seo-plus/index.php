<?php
// Silence is golden.

// Some useful tests for bbPress and BuddyPress conditions
// had to store the code somewhere so I wouldn't loose it :-)

/* // BuddyPress and bbPress Conditional Tests
if ( class_exists( 'BuddyPress' ) ) {
if ( is_buddypress() ) { echo '<small># is_buddypress<br></small>'; }
if ( bp_is_activity_directory() ) { echo '<small># bp_is_activity_directory<br></small>'; }
if ( bp_is_single_activity() ) { echo '<small># bp_is_single_activity<br></small>'; }
if ( bp_is_user() ) { echo '<small># bp_is_user<br></small>'; }
if ( bp_is_members_directory() ) { echo '<small># bp_is_members_directory<br></small>'; }
if ( bp_is_groups_directory() ) { echo '<small># bp_is_groups_directory<br></small>'; }
if ( bp_is_group() ) { echo '<small># bp_is_group<br></small>'; }
if ( bp_is_current_action( 'forum' ) && !bp_is_group_forum_topic() ) { echo '<small># bp_is_current_action( forum )<br></small>'; }
if ( bp_is_group_forum_topic() ) { echo '<small># bp_is_group_forum_topic<br></small>'; }
if ( bp_is_register_page() ) { echo '<small># bp_is_register_page<br></small>'; }
if ( bp_is_activation_page() ) { echo '<small># bp_is_activation_page<br></small>'; }
}

if ( class_exists( 'bbPress' ) ) {
if ( is_bbpress() ) { echo '<small>* is_bbpress<br></small>'; }
if ( bbp_is_forum_archive() ) { echo '<small>* bbp_is_forum_archive<br></small>'; }
if ( bbp_is_forum_category() ) { echo '<small>* bbp_is_forum_category<br></small>'; }
if ( bbp_is_single_forum() ) { echo '<small>* bbp_is_single_forum<br></small>'; }
if ( bbp_is_single_topic() ) { echo '<small>* bbp_is_single_topic<br></small>'; }
if ( bbp_is_topic_tag() ) { echo '<small>* bbp_is_topic_tag<br></small>'; }
if ( bbp_is_single_user() ) { echo '<small>* bbp_is_single_user<br></small>'; }
}

if ( class_exists( 'bbPress' ) ) {
if ( bbp_is_forum() ) { echo '<small>_ bbp_is_forum<br></small>'; }
if ( class_exists( 'BuddyPress' ) ) {
if ( bbp_is_forum_group_forum() ) { echo '<small>_ bbp_is_forum_group_forum<br></small>'; }
if ( bbp_is_forum_group_forum( bbp_get_forum_id() ) ) { echo '<small>_ bbp_is_forum_group_forum<br></small>'; }
}
if ( bbp_is_topic_archive() ) { echo '<small>_ bbp_is_topic_archive<br></small>'; }
if ( bbp_is_topic() ) { echo '<small>_ bbp_is_topic<br></small>'; }
}

if ( class_exists( 'BuddyPress' ) ) {
if ( bp_is_group_forum() ) { echo '<small>- bp_is_group_forum<br></small>'; }
if ( bp_is_group_single() ) { echo '<small>- bp_is_group_single<br></small>'; }
if ( bp_is_active( 'forums' ) ) { echo '<small>- bp_is_active( forums )<br></small>'; }
if ( bp_is_user_forums() ) { echo '<small>- bp_is_user_forums<br></small>'; }
if ( bp_is_groups_component() ) { echo '<small>- bp_is_groups_component<br></small>'; }
if ( bp_is_forums_component() ) { echo '<small>- bp_is_forums_component<br></small>'; }
#if ( bp_is_blog_page() ) { echo '<small> is bp_is_blog_page<br></small>'; }
#if ( !bp_is_blog_page() ) { echo '<small> !bp_is_blog_page<br></small>'; }
}
*/
?>